import EL{{properCase name}} from './src/{{name}}.vue'

EL{{properCase name}}.install = Vue => {
  Vue.component(EL{{properCase name}}.name, EL{{properCase name}})
}

export default EL{{properCase name}}
